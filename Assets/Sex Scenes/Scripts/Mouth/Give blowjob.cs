using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Giveblowjob : SexScenes
{
    public override string Text(BasicChar player, BasicChar other)
    {
        return base.Text(player, other);
    }
    public override void DoScene(playerMain player, BasicChar other)
    {
        base.DoScene(player, other);
    }
}
